# ops

Operational/private materials (applications, riders, budgets, drafts) live in the
private repo `dob-0/br_id_ge-ops`. This public repo keeps only public-safe ops
notes (`space-sync.md`, `needs-project-removal.md`). Public-facing pages are
drafted there and graduated here (repo root) when approved.
